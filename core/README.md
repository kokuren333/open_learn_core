# Core

This directory contains the reusable schemas, loaders, validation, graph utilities, quality gates, evidence coverage, renderer, build helpers, skills, templates, and configuration shared by every learning domain.

Core modules must remain domain-independent. Subject matter belongs under `domains/<domain>/`.
