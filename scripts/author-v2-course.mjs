import { mkdir, readdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";

// This file is a deterministic authoring aid. The JSON files it produces are the
// canonical, reviewable course source; no lesson text is kept in a prompt/cache.
const root = process.cwd();
const domainRoot = path.join(root, "domains", "linear-algebra");
const unitsRoot = path.join(domainRoot, "data", "units");
const conceptsRoot = path.join(domainRoot, "data", "concepts");

const profile = (ja, en, definition, intuition, method, example, boundary, answer, focus) => ({ ja, en, definition, intuition, method, example, boundary, answer, focus });

const profiles = {
  "scalar-introduction": profile("スカラーと数の役割", "Scalars and Numbers", "スカラーは、ベクトルを伸縮・反転したり、別のスカラーと演算したりする数である。", "ベクトルが方向を持つ材料なら、スカラーはその材料の量を調整するつまみである。2倍なら同じ向きに長くなり、-1倍なら向きが反転する。", "数の種類を確認し、ベクトルとスカラーを区別してから、加法・乗法の結果がどちらの種類かを判定する。", "u=(2,-1)にスカラー3を掛けると3u=(6,-3)となる。3は新しいベクトルではなく、各成分を同じ倍率で変える数である。", "スカラーをベクトルの成分と混同したり、負のスカラーを長さだけの変化と考えたりしない。-1倍では方向も反転する。", "3u=(6,-3)。スカラーは各成分に同じ数を掛ける。", "ベクトル計算の係数を読める"),
  "vector-introduction": profile("ベクトルを対象として見る", "What Is a Vector?", "ベクトルは、加法とスカラー倍を行える対象であり、R^nでは順序を持つn個の数の組で表す。", "矢印はベクトルの一つの見え方にすぎない。始点を動かしても成分の差が同じなら同じ自由ベクトルとして扱える。", "成分数と順序を確認し、同じ次元のベクトルだけを加え、成分ごとの演算を行う。", "u=(1,2), v=(-2,3)ならu+v=(-1,5)。第1成分と第2成分を同じ位置どうしで組み合わせる。", "ベクトルを点と同一視しない。点は位置、ベクトルは変位や演算対象であり、同じ成分の組でも文脈が異なる。", "u+v=(-1,5)。ベクトルの等しさは対応する全成分の一致で決まる。", "成分と演算を使ってベクトルを説明する"),
  "vector-notation": profile("ベクトルの記法と次元", "Vector Notation and Dimension", "ベクトルの次元は成分の個数であり、列ベクトル・行ベクトルの表記では形状と成分の順序を明示する。", "(a,b)と(b,a)は数字が同じでも別のベクトルである。順序は、どの座標方向にどれだけ進むかを記録するラベルになる。", "ベクトルを縦または横に並べ、成分数を数え、加法や行列積で形状が合うかを確認する。", "u=(2,-1,4)^Tは3次元列ベクトルで、uの第2成分は-1。v=(2,-1)とは次元が異なる。", "見た目が横長でも、行列計算では1×n行列とn×1行列を区別する必要がある。", "uはR^3のベクトルであり、v∈R^2とは加えられない。", "記法から次元・形状を読み取る"),
  "vector-addition": profile("ベクトルの加法", "Vector Addition", "同じ次元のベクトルの和は、対応する成分を加えて得られるベクトルである。", "矢印を先端からつなぐと、最初の始点から最後の終点への変位が和になる。成分計算と平行四辺形の図は同じ操作を別の言葉で表す。", "次元をそろえ、対応する成分を一つずつ加え、結果の次元と意味を確認する。", "u=(1,2), v=(3,-1)ならu+v=(1+3,2-1)=(4,1)。第1方向へ4、第2方向へ1の変位である。", "異なる次元のベクトルを加えたり、成分をずらして加えたりできない。", "u+v=(4,1)。成分ごとの加法は矢印の合成と一致する。", "図形と成分計算を往復する"),
  "scalar-multiplication": profile("スカラー倍", "Scalar Multiplication", "スカラーcによるベクトルvのスカラー倍cvは、vの全成分をc倍する演算である。", "正のcは方向を保った伸縮、負のcは方向反転を伴う伸縮、c=0は零ベクトルへの押しつぶしを表す。", "係数の符号と大きさを先に読み、全成分へ同じ係数を掛け、幾何的な変化を言葉で確認する。", "v=(2,-3)なら-2v=(-4,6)。長さは2倍になり、向きは反転する。", "一部の成分だけを変える操作ではない。c=0のときは方向が残るのではなく、零ベクトルになる。", "-2v=(-4,6)。全成分を同じ倍率で変える。", "係数の符号・零・倍率の意味を説明する"),
  "dot-product-and-norm": profile("内積・ノルム・距離", "Dot Product, Norm, and Distance", "R^nの内積はu·v=Σu_iv_i、ノルムは||u||=√(u·u)、距離は||u-v||で定義される。", "内積は二つの方向のそろい具合を一つの数に圧縮する。自分自身との内積から長さが生まれ、差のノルムから二点間の距離が生まれる。", "内積を成分積の和として計算し、u·uからノルム、u-vから距離を順に求める。", "u=(3,4), v=(1,0)ならu·v=3、||u||=5、||u-v||=√((2)^2+4^2)=√20。", "内積が0なら、標準内積のR^nでは直交を意味するが、内積の定義を変えれば判定も変わる。", "u·v=3、||u||=5、||u-v||=2√5。内積・長さ・距離を区別する。", "直交と距離の計算へつなぐ"),
  "linear-equations": profile("線形方程式系", "Systems of Linear Equations", "線形方程式系は、未知数を一次式で結んだ複数の方程式を同時に満たす解を求める問題である。", "一つの方程式は平面上の直線、二つの方程式は交点候補を表す。解とは、すべての制約が同時に成立する点である。", "方程式を拡大係数行列へ写し、同値な行操作で簡単な形にして、主変数と自由変数を読み取る。", "x+y=3, x-y=1を加えると2x=4、x=2、代入してy=1。交点は(2,1)である。", "方程式を一つずつ別々に解いてはいけない。解なし・一意解・無限解の三通りがあり得る。", "解は(x,y)=(2,1)。二式を同時に満たすか代入で検算する。", "行列と消去法の動機を作る"),
  "matrix-introduction": profile("行列を方程式の表にする", "Introduction to Matrices", "行列は数を長方形に配置した対象で、線形方程式系や線形写像の係数を規則正しく保持する。", "行列は計算そのものではなく、係数と入力・出力の対応を並べた表である。表にすると、同じ消去操作を機械的に追える。", "行と列の意味を定め、m×nの形状を確認し、行列とベクトルの積がどの成分を作るかを読む。", "A=[[1,2],[3,4]], x=(x1,x2)^TならAx=(x1+2x2,3x1+4x2)^T。各行が一つの線形結合を計算する。", "行列の行列式を単なる全要素の積と考えない。まず行・列・形状を区別する。", "Aは2×2行列で、Axの第1成分はAの第1行とxの内積。", "行列を係数構造として読める"),
  "matrix-operations": profile("行列の演算", "Matrix Operations", "行列の加法は同じ形状の対応成分の和、積ABはAの行とBの列の内積で定まる。", "行列積は二つの処理を順に適用する合成であり、表の要素をばらばらに掛ける操作ではない。", "加法では形状を一致させ、積ではAの列数とBの行数を確認し、各成分を行・列の内積で計算する。", "A=[[1,2],[0,1]], B=[[2,0],[3,1]]ならAB=[[8,2],[3,1]]。BA=[[2,4],[3,7]]で一般に異なる。", "積は交換法則を満たさない。ABが定義できてもBAが定義できるとは限らない。", "AB=[[8,2],[3,1]]。内側の次元が一致し、外側の次元が結果になる。", "形状と合成の関係を理解する"),
  "gaussian-elimination": profile("ガウス消去法", "Gaussian Elimination", "ガウス消去法は、行交換・非零行のスカラー倍・他行の倍数加減という可逆な行操作で連立方程式を簡約する方法である。", "各列の下にある係数を0にして、未知数の依存関係を階段状に露出させる。見た目を変える操作ではなく、解集合を保つ操作である。", "拡大係数行列を作り、左からピボットを選び、下を0にし、最後に後退代入する。零行や交換不能なピボットも確認する。", "x+y=3, 2x+3y=8の拡大行列でR2←R2-2R1を行うと[0,1|2]、よってy=2,x=1。", "ピボットが0なら行交換を試す。全係数0=非零定数の行は解なし、零行は冗長な条件を示す。", "解は(1,2)。各行操作が解集合を保つ理由を一つずつ説明する。", "アルゴリズムと解釈を結び付ける"),
  "rref-and-pivots": profile("階段形・主変数・自由変数", "Echelon Form and Pivots", "主行基本形では各ピボットが1で、その列の他の成分が0になり、ピボット列と自由列から解の構造を読める。", "ピボットは、その列で新しい制約を代表する最初の非零成分である。自由変数は、方程式だけでは値が決まらない方向を表す。", "行を上から整え、各ピボットを1にし、ピボット列を掃き出して、自由変数をパラメータとして解を記述する。", "x+2z=3, y-z=1のRREFからz=tと置けばx=3-2t,y=1+t。解は一つの直線として表される。", "ピボット数を未知数の個数と取り違えない。自由変数があれば解は一意ではない。", "z=t, (x,y,z)=(3-2t,1+t,t)。自由変数が解の自由度を決める。", "rank・null spaceへの橋を作る"),
  "inverse-and-lu": profile("逆行列とLU型分解", "Inverses and LU Factorization", "正方行列Aの逆行列A^{-1}はAA^{-1}=Iを満たす行列で、LU分解はAを下三角行列Lと上三角行列Uの積として扱う計算法である。", "逆行列は『入力を元に戻す処理』、LUは一度の消去を再利用可能な二段階の処理へ分解する見方である。", "逆行列は拡大行列[A|I]をRREFにし、LUでは消去係数をLに保存してLy=b, Ux=yの二段階で解く。", "A=[[2,1],[4,3]]ではR2←R2-2R1、L=[[1,0],[2,1]], U=[[2,1],[0,1]]。detA=2なので逆行列も存在する。", "特異行列には逆行列がない。数値計算では逆行列を明示的に作るより、分解して連立方程式を解く方が安定・効率的なことが多い。", "A=LUを確認し、解法を『逆行列を掛ける』だけで終わらせず、前進・後退代入で示す。", "消去・計算量・数値安定性へつなぐ"),
  "vector-spaces": profile("ベクトル空間の公理", "Vector Spaces", "ベクトル空間は、集合と加法・スカラー倍が閉じており、結合則・零元・逆元・分配法則などの公理を満たす構造である。", "空間を矢印の集合ではなく、同じ種類の足し算と倍率操作が安全にできる世界として見る。多項式や関数も同じ規則を満たせばベクトルになる。", "候補集合と演算を示し、閉性を確認した後、必要な公理のうち破れやすいものを反例で検査する。", "次数2以下の多項式全体は、和と実数倍で次数2以下に留まるのでベクトル空間になる。", "『同じ見た目』ではなく演算とスカラーの体を指定する。正の数だけの集合は加法逆元を含まず、通常はベクトル空間でない。", "多項式の和・スカラー倍が閉じていることを確認し、係数組との対応を説明する。", "抽象定義を具体的な集合へ適用する"),
  "subspaces": profile("部分空間", "Subspaces", "ベクトル空間Vの部分集合Wが部分空間であるとは、零ベクトルを含み、加法とスカラー倍に閉じていることである。", "部分空間は大きな空間の中にある、演算をしても外へ飛び出さない小さな世界である。原点を通る直線・平面が典型例になる。", "零ベクトルを確認し、任意のu,v∈Wとスカラーc,dに対してcu+dv∈Wを示す。", "W={(x,y):x+y=0}は、(t,-t)と書け、二つの元の線形結合も同じ形なのでR²の部分空間。", "原点を通らない直線は線形結合の零元を含まないため部分空間でない。条件を満たす集合の部分集合というだけでは不十分。", "Wはspan{(1,-1)}であり、R²の部分空間。", "span・kernel・column spaceの準備をする"),
  "subspaces": profile("部分空間", "Subspaces", "ベクトル空間Vの部分集合Wが部分空間であるとは、零ベクトルを含み、加法とスカラー倍に閉じていることである。", "部分空間は大きな空間の中にある、演算をしても外へ飛び出さない小さな世界である。原点を通る直線・平面が典型例になる。", "零ベクトルを確認し、任意のu,v∈Wとスカラーc,dに対してcu+dv∈Wを示す。", "W={(x,y):x+y=0}は、(t,-t)と書け、二つの元の線形結合も同じ形なのでR²の部分空間。", "原点を通らない直線は線形結合の零元を含まないため部分空間でない。条件を満たす集合の部分集合というだけでは不十分。", "Wはspan{(1,-1)}であり、R²の部分空間。", "span・kernel・column spaceの準備をする"),
  "linear-transformations": profile("線形写像", "Linear Transformations", "線形写像T:V→WはT(u+v)=T(u)+T(v)、T(cv)=cT(v)を満たし、加法とスカラー倍の構造を保つ写像である。", "線形写像は、原点を保ちながら空間を伸ばす・回す・つぶす規則である。入力を分解して処理した結果が、先に足してから処理した結果と一致する。", "零ベクトルの像を確認し、加法性と斉次性を一般のu,v,cで確認してから、標準基底の像を行列へ並べる。", "T(x,y)=(2x-y,x)ではT(u+v)=T(u)+T(v)、T(3u)=3T(u)が成分計算で確かめられる。", "平行移動T(x)=x+b（b≠0）は原点を保たず線形でない。線形写像と一般の関数を区別する。", "T(0)=0で、和とスカラー倍を保つのでTは線形。", "kernel・imageと行列表現へ接続する"),
  "kernel-and-image": profile("核と像", "Kernel and Image", "線形写像T:V→Wの核ker(T)はT(v)=0となる入力全体、像im(T)はT(v)として得られる出力全体で、どちらも部分空間である。", "核は写像が見分けられない入力方向、像は実際に到達できる出力方向である。つぶれる方向と残る範囲を分けて見る。", "T(v)=0を解いて核を求め、出力をTの列の線形結合として像を求める。行列なら零空間と列空間を使う。", "A=[[1,2],[2,4]]ではker(A)=span{(-2,1)}、im(A)=span{(1,2)}。一方向がつぶれ、出力も一方向に限られる。", "核を行列の対角成分だけで判断しない。像を『行列の全要素』と呼ばず、列のspanとして記述する。", "ker(A)とim(A)を方程式と生成元の両方で示せる。", "rank-nullityと可逆性へ接続する"),
  "change-of-basis": profile("基底変換", "Change of Basis", "同じベクトルの座標を基底Bから基底Cへ変換するには、基底行列を用いて[v]_C=P_{C←B}[v]_Bと表す。", "物体を変えずに、ものさしだけを取り替える。数字は変わるが、基底ベクトルの線形結合として再構成した結果は同じベクトルになる。", "基底行列を列ベクトルで作り、B座標から実ベクトルへ戻し、Cの係数を未知数として連立方程式を解く。", "B={(1,0),(0,1)}, C={(1,1),(1,-1)}で[v]_B=(3,1)ならv=(3,1)=2(1,1)+1(1,-1)、よって[v]_C=(2,1)。", "座標変換行列の向きを逆にしない。P_{C←B}はB座標を入力し、C座標を出力する。", "P_{C←B}[[3],[1]]=[[2],[1]]。変換後にCの基底で再構成して検算する。", "行列表示・相似へつなぐ"),
  "coordinate-maps": profile("座標写像", "Coordinate Maps", "基底Bに対する座標写像φ_Bは、ベクトルvを一意な係数列[v]_Bへ送る線形同型である。", "座標写像は、抽象的なベクトルを計算しやすい数の列へ翻訳する辞書である。辞書を変えると表現は変わるが対象は変わらない。", "基底での係数を未知数として置き、元のベクトルを再構成して一致を確認する。写像の加法・スカラー倍保存も確認する。", "B={(1,1),(1,-1)}でv=(4,2)ならa+b=4,a-b=2を解き[a]_B=(3,1)。", "基底でない集合では座標が存在しないか一意でない。標準座標とB座標を同じ記号の数字として扱わない。", "φ_B(4,2)=(3,1)。座標は選択した基底に依存する。", "抽象ベクトルと行列計算を接続する"),
  "composition-and-similarity": profile("写像の合成と相似", "Composition and Similarity", "線形写像Sの後にTを適用する合成T∘Sは線形で、基底を変えた同じ線形写像の行列はP^{-1}APで相似である。", "処理を二つ続けて行うと、一つの処理にまとめられる。相似行列は同じ作用を別の座標言語で記録したものである。", "合成の順序を右から左へ適用し、基底行列Pの向きを確認して表現行列を変換する。", "S(x,y)=(x+y,y), T(x,y)=(x,2y)ならT∘S(x,y)=(x+y,2y)。順序を逆にすると一般に異なる。", "ABとBAを同一視しない。相似は行列要素が同じという意味ではなく、同じ線形作用の座標表現が変わったという意味。", "合成は線形で、行列表現は積TS。", "固有値・対角化への準備をする"),
  "orthogonality": profile("直交性", "Orthogonality", "標準内積に関してu·v=0ならuとvは直交し、非零直交ベクトルは線形独立である。", "直交は『互いの方向成分を測ると0』という関係で、直角という図形だけでなく関数空間などにも広がる。", "内積を計算して0かを確認し、必要ならピタゴラス型の長さ関係や独立性へ解釈を進める。", "u=(1,2), v=(2,-1)ならu·v=2-2=0。二本とも非零なので線形独立でもある。", "直交でない独立なベクトルも多い。内積の種類が変われば『直交』の意味も変わる。", "u·v=0なので直交。長さは別に計算して関係を説明する。", "射影・直交補空間の基礎を作る"),
  "orthogonal-complement": profile("直交補空間", "Orthogonal Complements", "部分空間Wの直交補空間W^⊥は、Wのすべてのベクトルと内積0になるベクトル全体である。", "W^⊥は、Wの方向成分を一切持たない方向の集合である。R²の直線に対する補空間は、原点を通る垂直な直線になる。", "Wの生成ベクトルとの内積を0とする方程式を立て、解集合が部分空間であることを確認する。", "W=span{(1,1)}なら(x,y)·(1,1)=0、つまりx+y=0なのでW^⊥=span{(1,-1)}。", "W^⊥をWの『集合差』と考えない。直交補は内積で定まる。またW自身と同じとは限らない。", "W^⊥はspan{(1,-1)}で、R²を二つの直交方向に分ける。", "projection・least squaresへの橋を作る"),
  "projection": profile("直交射影", "Orthogonal Projection", "部分空間Wへの直交射影proj_W(v)は、vからWへの誤差がW^⊥に入るようなW内のベクトルである。", "斜めの点を、指定した直線や平面へ最も近い点として落とす。残った誤差は対象空間の方向と直交する。", "直交基底ならproj_W(v)=Σ(v·q_i)q_i、一般の一本の方向uなら((v·u)/(u·u))uを使う。", "v=(2,1), u=(1,1)なら係数=(3/2)、射影=(3/2,3/2)、誤差=(1/2,-1/2)でuと直交する。", "uを単位ベクトルと仮定して分母を落とさない。射影先がWであることと、誤差が直交することを検算する。", "proj_W(v)=(3/2,3/2)。距離最小性は誤差の直交性から説明できる。", "最小二乗へつなぐ"),
  "orthonormal-bases": profile("正規直交系と正規直交基底", "Orthonormal Sets and Bases", "ベクトル集合が正規直交系であるとは、各ベクトルのノルムが1で、異なる二本の内積が0であることをいう。", "互いに干渉しない単位方向をそろえると、座標係数が内積を一回計算するだけで読める。", "各ノルムを1にし、相互内積を0と確認し、基底本数が空間の次元と一致するかを確認する。", "q1=(1,0), q2=(0,1)はR²の正規直交基底。v=(3,4)の係数はv·q1=3,v·q2=4。", "直交しているだけでは単位長とは限らず、単位長だけでも互いに直交するとは限らない。", "{q1,q2}は正規直交基底で、[v]_Q=(3,4)。", "Gram-SchmidtとQRの準備をする"),
  "gram-schmidt": profile("Gram–Schmidt法", "The Gram–Schmidt Process", "Gram–Schmidt法は、線形独立なベクトル列から同じspanを持つ正規直交基底を逐次的な射影除去で作る方法である。", "新しいベクトルから、すでに選んだ方向の影を引く。残った成分は既知の方向と直交し、最後に長さ1へ正規化する。", "u1を正規化し、u2からu1方向への射影を引き、得られたベクトルを正規化する。高次元ではこの手順を繰り返す。", "v1=(1,1), v2=(1,0)。q1=(1,1)/√2、v2-(v2·q1)q1=(1/2,-1/2)、q2=(1,-1)/√2。", "入力が線形従属だと残差が零になり正規化できない。丸め誤差が大きい場合は改良版が必要になることがある。", "q1=(1,1)/√2,q2=(1,-1)/√2で、元の二本と同じ平面をspanする。", "QR分解と数値計算へ接続する"),
  "qr-least-squares": profile("QR分解と最小二乗", "QR Factorization and Least Squares", "フル列ランクの行列AはA=QR（Qの列が正規直交、Rが上三角）と分解でき、最小二乗問題はRx=Q^Tbで解ける。", "過剰決定のAx=bを完全に満たせないとき、bをAの列空間へ直交射影して最も近い予測を選ぶ。QRはこの射影を安定に計算する。", "Aの列をGram–SchmidtでQにし、R=Q^TAを作り、Q^T Ax=Q^Tbから上三角方程式を解く。", "A=[(1,0),(1,1)]を列に持つ場合、Qを作れば、観測bに対する近似係数はQ^TbをRで後退代入して得られる。", "正規方程式を使うときもAの列ランク条件を確認する。ランク不足なら解は一意でなく、正則化など別の扱いが必要。", "残差r=b-AxはAの列空間と直交し、||r||が最小になる。", "投影・データフィッティングの実用へつなぐ"),
  "determinant-intuition": profile("行列式の直観", "Determinant Intuition", "行列式det(A)は、線形写像が向き付き面積・体積を何倍するかを表すスカラーである。", "単位正方形をAで写した平行四辺形の面積が|det A|、向きが反転すれば符号が変わる。空間を押しつぶすと面積・体積は0になる。", "標準基底の像を図にし、2×2ではad-bcを計算し、絶対値を面積倍率・符号を向きとして読む。", "A=[[2,0],[0,3]]は面積を6倍しdetA=6。A=[[1,0],[0,-1]]は面積を保つが向きを反転しdetA=-1。", "行列式の幾何的意味だけで定義を置き換えない。非正方行列には通常このdetは定義されない。", "detA=6は面積倍率6を意味し、detA=0なら平面を線へ押しつぶす。", "定義・計算・可逆性へ進む"),
  "determinant-definition": profile("行列式の定義", "The Determinant", "n×n行列の行列式は、置換の符号と各行から一つずつ選ぶ積の総和で定義される交代多重線形関数である。", "2×2のad-bcは、二つの対応の積を向きの符号付きで引いたもの。高次元では同じ『全ての対応を符号付きで足す』原理になる。", "小行列展開または置換定義を使い、行・列を固定して項を整理する。定義と計算法を混同しない。", "A=[[a,b],[c,d]]ならdetA=ad-bc。A=[[1,2],[3,4]]では4-6=-2。", "行列式を対角成分の積だけとしない。余因子展開では符号と小行列を落とさない。", "det[[1,2],[3,4]]=-2。定義に基づく計算であることを確認する。", "性質と効率的計算へつなぐ"),
  "determinant-properties": profile("行列式の性質", "Properties of Determinants", "行列式はdet(AB)=det(A)det(B)、行交換で符号反転、行の倍数加算で不変、転置で不変という性質を持つ。", "基本行操作が面積・体積倍率へどう影響するかを追えば、複雑な行列式を三角行列まで簡約できる。", "行操作ごとの倍率を記録し、三角行列の対角積を計算し、記録した因子を戻す。", "Aを行交換一回でUにし、det(U)=6ならdet(A)=-6。行の他行倍加はdetを変えない。", "行をc倍したときだけdetがc倍になる。全体を同時に行う操作の影響を一因子と取り違えない。", "det(A^T)=det(A)、det(AB)=det(A)det(B)を小行列の例で検算する。", "計算・逆行列・固有値へ接続する"),
  "determinant-computation": profile("行列式の計算", "Computing Determinants", "行列式は余因子展開や行基本変形を使って計算でき、三角行列では対角成分の積に等しい。", "大きな式を直接展開せず、零を増やして計算を短くする。行操作の記録が、正しい答えを復元するための帳簿になる。", "疎な行・列を選び、必要なら行交換して消去し、交換・スカラー倍の影響を最後に補正する。", "A=[[1,2,0],[0,3,4],[0,0,5]]は上三角なのでdetA=1·3·5=15。", "行列式の計算途中で方程式の解を変える操作と、行列式の値を変える操作を区別する。", "detA=15。三角形状を見つけたら対角積を使う。", "逆行列・可逆性・体積倍率を実計算する"),
  "determinant-invertibility": profile("行列式と可逆性", "Determinants and Invertibility", "正方行列Aはdet(A)≠0のとき、かつそのときに限り可逆であり、det(A)=0なら零でないベクトルを零へ送る方向がある。", "det=0は空間を低い次元へ押しつぶした状態で、情報が失われるため元に戻せない。", "detを計算し、0かどうかから可逆性を判断し、必要ならAx=0の非自明解や逆行列の存在で裏付ける。", "A=[[1,2],[2,4]]はdet=0。2列目=2×1列目なのでA(−2,1)^T=0、逆行列はない。", "det≠0は数値計算で『安全』を意味しない。非常に小さいdetや条件数の問題は別に扱う。", "detA=0なので非可逆。従属な列が情報を重複させている。", "rank・線形方程式・数値安定性をつなぐ"),
  "determinant-volume": profile("行列式と面積・体積", "Determinants and Volume Scaling", "n次元の線形写像Aは、測度を|det A|倍し、det Aの符号は基底の向きの反転を示す。", "単位立方体を押す・伸ばす・せん断する操作を分解すると、体積倍率が各操作の積として見える。", "標準立方体の像を考え、対角伸縮・せん断・交換の倍率を掛け合わせてdetの意味を解釈する。", "diag(2,3,4)は体積を24倍する。せん断[[1,1],[0,1]]は面積を保ちdet=1。", "面積や体積の倍率は絶対値で、向きの情報は符号に残る。非線形写像へそのまま一般化しない。", "det diag(2,3,4)=24。せん断が形を変えても面積を保つことを説明する。", "固有値の積・応用幾何へ接続する"),
  "eigenvalue-eigenvector": profile("固有値と固有ベクトル", "Eigenvalues and Eigenvectors", "A v=λvを満たす非零ベクトルvを固有ベクトル、対応するスカラーλを固有値という。", "多くの方向が混ざる線形変換の中で、向きが変わらず伸縮率だけが変わる特別な方向を探す。", "Av=λvを成分方程式にし、v≠0の条件を残してλとvを求め、元の式へ代入して検算する。", "A=diag(2,3)ではe1=(1,0)がλ=2、e2=(0,1)がλ=3の固有ベクトル。", "零ベクトルはどんなλでも式を満たすが固有ベクトルではない。固有ベクトルはスカラー倍までしか一意でない。", "Ae1=2e1、Ae2=3e2。固有空間は対応する固有値の全解集合。", "特性多項式と対角化へ進む"),
  "characteristic-polynomial": profile("特性多項式", "Characteristic Polynomial", "正方行列Aの特性多項式p(λ)=det(A-λI)の根がAの固有値である。", "Av=λvに非零解があるには、A-λIが可逆であってはいけない。その条件をdet=0という一つの式に圧縮する。", "A-λIを作り、行列式を計算し、根を求め、各根ごとに(A-λI)v=0を解く。", "A=[[2,1],[0,3]]ならp(λ)=(2-λ)(3-λ)、固有値は2,3。", "p(λ)の符号規約はdet(A-λI)とdet(λI-A)で変わるが、根は同じ。多項式の根と固有ベクトルを混同しない。", "固有値2,3を得たら、それぞれの固有空間を別に求める。", "重複固有値・重複度へ接続する"),
  "eigenspace-and-multiplicity": profile("固有空間と重複度", "Eigenspaces and Multiplicities", "固有値λの固有空間E_λ=ker(A-λI)は線形空間であり、代数的重複度は特性多項式の根の重複、幾何的重複度はdim E_λである。", "同じ伸縮率を持つ方向が一本とは限らない。多項式が根を何回数えるかと、実際に何方向の独立な固有ベクトルがあるかを分ける。", "特性多項式の根の回数を数え、各λの零空間を解いて次元を測り、幾何的重複度≤代数的重複度を確認する。", "A=2I_2はλ=2の代数的重複度2、固有空間全体R²で幾何的重複度2。", "重複度が同じでも自動的に対角化できるとは限らない。固有空間の次元を実際に計算する。", "A=2Iなら二つの独立な固有ベクトルを選べる。", "対角化可能性の判定を準備する"),
  "diagonalization": profile("対角化", "Diagonalization", "Aがn本の線形独立な固有ベクトルを持つとき、A=PDP^{-1}と対角化でき、Dの対角成分は固有値である。", "適切な固有ベクトルを座標軸に選び直すと、複雑な作用が各軸の独立な伸縮に分解される。", "固有ベクトルを列に並べPを作り、対応する固有値をDに並べ、AP=PDを検算する。", "A=diag(2,3)はP=I,D=Aで既に対角化。一般にA^k=PD^kP^{-1}と計算できる。", "固有値があるだけでは不十分。固有ベクトルが空間をspanする本数必要で、重複不足なら対角化できない。", "Pの列が独立でAP=PDならA=PDP^{-1}。", "相似・対角化・スペクトル定理へ接続する"),
  "symmetric-spectral-theorem": profile("対称行列とスペクトル定理", "Symmetric Matrices and the Spectral Theorem", "実対称行列は、互いに直交する固有ベクトルからなる正規直交基底を持ち、A=QΛQ^Tと直交対角化できる。", "対称性は、異なる固有値の方向が直交するという強い秩序を生む。斜めの変換を、直交座標で独立な伸縮として読む。", "固有値・固有ベクトルを求め、各固有空間で正規化し、Q^TQ=IとQΛQ^T=Aを確認する。", "A=[[2,1],[1,2]]の固有値3,1に対し、q1=(1,1)/√2,q2=(1,-1)/√2が直交正規基底を作る。", "任意の行列が直交対角化できるわけではない。実対称性という仮定を明示する。", "Q^TQ=Iで、A=Q diag(3,1) Q^T。", "二次形式・正定値性へつなぐ"),
  "positive-definite-matrices": profile("正定値行列", "Positive-Definite Matrices", "実対称行列Aが正定値であるとは、任意の非零xに対してx^TAx>0であることをいう。", "x^TAxは方向xに沿った二次的なエネルギーや曲率を測る。全方向で正なら、原点が谷の底のように安定している。", "対称性を確認し、固有値がすべて正か、2×2なら主座小行列式が正かを使い分ける。", "A=diag(2,3)ならx^TAx=2x1²+3x2²>0（x≠0）なので正定値。", "対称性なしでは固有値の符号だけで同じ判定をしてはいけない。半正定値は0を許す別概念。", "非零xでは2x1²+3x2²>0。よってAは正定値。", "二次形式・最適化・数値計算へ接続する"),
  "quadratic-forms": profile("二次形式", "Quadratic Forms", "二次形式はq(x)=x^TAxで表され、対称部分だけが値に影響し、行列の固有方向で曲率を解釈できる。", "円・楕円・双曲線などの形は、方向ごとの二次的な伸び縮みの組合せとして現れる。", "Aを対称部分(A+A^T)/2へ置き換え、固有基底でqを対角形にし、固有値の符号で形を読む。", "q(x,y)=2x²+4xy+5y²はA=[[2,2],[2,5]]で表せ、対称行列なので固有値の符号から正定値性を調べられる。", "交差項の係数を行列へ写すときは2a12がxy係数になる。図形の判定を係数の見た目だけで行わない。", "q=x^TAxを展開して元の式へ戻ることで係数配置を検算する。", "SVD・PCA・最適化の幾何へつなぐ"),
  "svd-intuition": profile("特異値分解の直観", "Singular Value Decomposition", "任意の実m×n行列AはA=UΣV^Tと分解でき、U,Vは直交行列、Σの非負対角成分が特異値である。", "SVDは、入力を直交座標で回転・反射し、方向ごとに異なる倍率で伸縮し、出力側の直交座標へ移す三段階の機械である。", "A^TAの固有ベクトルをVに選び、特異値σ=√λを求め、対応する左特異ベクトルをAv/σで作る。", "A=diag(3,1)ではU=V=I,Σ=diag(3,1)。第1方向を3倍、第2方向を1倍する。", "SVDを単に矩形行列の固有分解と呼ばない。左右の異なる空間と、A^TA・AA^Tの関係を区別する。", "UΣV^Tを掛け戻してAを再構成し、σ_i≥0と並び順を確認する。", "低ランク近似とPCAへ進む"),
  "low-rank-approximation": profile("低ランク近似", "Low-Rank Approximation", "SVDの特異値を大きい順に残す切断近似は、指定ランクの行列の中でフロベニウスノルム誤差を最小にする。", "画像や表の細かな変動を捨て、主要な方向だけを残す。ランクは保存する独立なパターン数の上限になる。", "SVDを取り、上位k個のσと対応する特異ベクトルだけでA_kを作り、誤差が残した特異値で決まることを読む。", "A=U diag(5,2,0.5)V^Tをランク1近似するとA1=5u1v1^T、二乗誤差は2²+0.5²。", "近似は元の行列と同じではない。特異値の切断が最良という主張にはノルムとランク条件を添える。", "A1は最大の特異値方向だけを残し、Frobenius誤差の二乗は残りの特異値二乗和。", "圧縮・PCAの橋を作る"),
  "pca-connection": profile("PCAと線形代数", "PCA and Linear Algebra", "中心化したデータ行列の共分散行列の固有ベクトルは主成分方向を与え、SVDの右特異ベクトルと対応する。", "雲状に散ったデータを、最もばらつく軸から順に回転して見直す。PCAはデータの中心を移してから方向を探す。", "各特徴量の平均を引いてX_cを作り、X_c^TX_cの固有ベクトルまたはX_cのSVDから主方向を求める。", "点(1,1),(2,2),(3,3)は中心化すると直線方向(1,1)に全分散が集中し、第一主成分になる。", "中心化しないデータでは平均の位置が第一方向を歪めることがある。標準化するかどうかも前処理の選択である。", "平均を引いたX_cの共分散最大固有ベクトルが第一主成分。", "低次元表現・近似と結び付ける"),
  "markov-matrices": profile("Markov行列", "Markov Matrices", "確率ベクトルを保つMarkov遷移行列は、確率の流れを線形写像として表し、定常分布は固有値1の固有ベクトルとして現れる。", "ページ間の移動や状態遷移を繰り返すと、確率の山が行列の作用で移動する。長期的に変わらない分布が定常分布である。", "確率の向きを列ベクトルか行ベクトルか固定し、非負性と和1を確認し、Av=vを解いて定常分布を正規化する。", "P=[[0.8,0.3],[0.2,0.7]]を列確率の規約で使うなら、v=Pvとv1+v2=1からv=(3/5,2/5)を得る。", "行和1と列和1は規約が違う。固有値1があっても、収束には既約性・周期性など追加条件が必要。", "P v=vかつ成分非負・総和1を検算する。", "固有値・反復・応用例を統合する"),
  "graph-network-matrices": profile("グラフとネットワーク行列", "Graph and Network Matrices", "グラフの隣接行列・次数行列・ラプラシアンは接続関係を線形代数へ写し、ラプラシアンの零空間は連結成分の構造を反映する。", "点と辺の図を表に翻訳すると、近傍の集計や流れの保存を行列積で計算できる。", "頂点の順序を固定して隣接行列を作り、次数行列DとL=D-Aを計算し、Lx=0の解を解釈する。", "三頂点の道1-2-3ではA=[[0,1,0],[1,0,1],[0,1,0]],D=diag(1,2,1)。", "隣接行列が対称なのは無向グラフのとき。頂点順序を変えると行列は変わるが、グラフ構造は同じ。", "Lの各行和は0で、定数ベクトルはLの零空間に入る。", "固有値・拡散・応用へつなぐ"),
  "differential-and-fourier": profile("微分方程式・Fourierとの接続", "Differential Equations and Fourier Connections", "線形微分方程式では、微分作用を行列や線形作用素として扱い、固有関数・固有ベクトルによるモード分解が時間発展を簡約する。", "複雑な信号や運動を、単純な振動モードの重ね合わせとして見る。各モードは固有値に応じて増減・振動する。", "線形性を確認し、簡単な固有モードを代入して時間因子を求め、重ね合わせの原理で一般解を組み立てる。", "x'(t)=Ax(t)でAの固有対(A v=λv)があれば、x(t)=e^{λt}vは一つのモードになる。", "固有ベクトルが足りない場合や非線形項がある場合は、単純な対角化だけでは解けない。", "固有モードごとに時間変化e^{λt}が分離されることを代入で確認する。", "線形代数を応用へ運ぶ"),
  "coordinates": profile("座標", "Coordinates", "基底Bに関するベクトルvの座標[v]_Bは、vをBのベクトルの線形結合で表した係数列である。", "座標はベクトルそのものではなく、選んだものさしに対する読み上げである。ものさしが変われば数字は変わるが、再構成される対象は同じである。", "基底ベクトルを列に並べた行列で係数方程式を立て、解を基底座標として再構成で検算する。", "B={(1,1),(1,-1)}でv=(4,2)ならa+b=4,a-b=2から[v]_B=(3,1)。", "標準座標を任意の基底座標として流用しない。基底でない集合では座標の存在・一意性が保証されない。", "[v]_B=(3,1)で、3(1,1)+1(1,-1)=(4,2)。", "基底変換と線形写像へ接続する"),
  "rank": profile("階数", "Rank", "行列Aの階数rank(A)は、列空間または行空間の次元であり、ピボットの個数に等しい。", "階数は、入力の中で出力へ残る独立な方向の数を数える。列が重複していれば新しい情報を増やさない。", "RREFのピボット数を数えるか、独立な列を選んでそのspanの次元を求める。", "Aの列c1=(1,0,1),c2=(0,1,1),c3=c1+c2なら、独立な列は二本なのでrank(A)=2。", "行数や列数をそのまま階数としない。ゼロ行や従属列がある場合はピボットを実際に数える。", "rank(A)=2。c3は新しい方向を加えない。", "nullity・rank-nullity・線形写像へ接続する"),
  "numerical-stability": profile("数値安定性", "Numerical Stability", "数値安定性は、入力や丸め誤差の小さな変化が計算結果へ過度に増幅されない性質であり、条件数とアルゴリズムの安定性を区別する。", "同じ数学問題でも、細い方向を持つ行列では少しの測定誤差が解を大きく動かす。問題の敏感さと解法の悪さは別の原因である。", "条件数・残差・相対誤差を確認し、可能なら逆行列の明示よりQRやLUなど構造を利用した解法を選ぶ。", "A=diag(1,10^{-6})では、第一方向と第二方向で倍率が大きく異なり、第二成分の相対誤差が増幅されやすい。", "detが非零でも条件数が大きければ数値的に扱いにくい。丸め誤差を『数学的な非可逆性』と混同しない。", "残差||Ax-b||が小さいことと解の相対誤差が小さいことを別に報告する。", "SVD・実データ・計算実装を締めくくる")
};

const unitMeta = new Map([
  ["scalar-introduction", ["module-prerequisites-vectors", 0]], ["vector-introduction", ["module-prerequisites-vectors", 1]], ["vector-notation", ["module-prerequisites-vectors", 2]], ["vector-addition", ["module-prerequisites-vectors", 3]], ["scalar-multiplication", ["module-prerequisites-vectors", 4]], ["dot-product-and-norm", ["module-prerequisites-vectors", 5]],
  ["linear-equations", ["module-systems-matrices", 0]], ["matrix-introduction", ["module-systems-matrices", 1]], ["matrix-operations", ["module-systems-matrices", 2]], ["gaussian-elimination", ["module-systems-matrices", 3]], ["rref-and-pivots", ["module-systems-matrices", 4]], ["inverse-and-lu", ["module-systems-matrices", 5]],
  ["vector-spaces", ["module-vector-spaces", 0]], ["subspaces", ["module-vector-spaces", 1]], ["linear-combination", ["module-vector-spaces", 2]], ["span", ["module-vector-spaces", 3]], ["linear-independence", ["module-vector-spaces", 4]], ["basis-definition", ["module-vector-spaces", 5]], ["dimension-and-rank", ["module-vector-spaces", 6]], ["coordinate-vectors", ["module-vector-spaces", 7]],
  ["linear-transformations", ["module-linear-transformations", 0]], ["kernel-and-image", ["module-linear-transformations", 1]], ["matrix-representation", ["module-linear-transformations", 2]], ["coordinate-maps", ["module-linear-transformations", 3]], ["change-of-basis", ["module-linear-transformations", 4]], ["composition-and-similarity", ["module-linear-transformations", 5]],
  ["orthogonality", ["module-orthogonality", 0]], ["orthogonal-complement", ["module-orthogonality", 1]], ["projection", ["module-orthogonality", 2]], ["orthonormal-bases", ["module-orthogonality", 3]], ["gram-schmidt", ["module-orthogonality", 4]], ["qr-least-squares", ["module-orthogonality", 5]],
  ["determinant-intuition", ["module-determinants", 0]], ["determinant-definition", ["module-determinants", 1]], ["determinant-properties", ["module-determinants", 2]], ["determinant-computation", ["module-determinants", 3]], ["determinant-invertibility", ["module-determinants", 4]], ["determinant-volume", ["module-determinants", 5]],
  ["eigenvalue-eigenvector", ["module-eigenvalues", 0]], ["characteristic-polynomial", ["module-eigenvalues", 1]], ["eigenspace-and-multiplicity", ["module-eigenvalues", 2]], ["diagonalization", ["module-eigenvalues", 3]], ["symmetric-spectral-theorem", ["module-eigenvalues", 4]], ["positive-definite-matrices", ["module-eigenvalues", 5]],
  ["quadratic-forms", ["module-applications", 0]], ["svd-intuition", ["module-applications", 1]], ["low-rank-approximation", ["module-applications", 2]], ["pca-connection", ["module-applications", 3]], ["markov-matrices", ["module-applications", 4]], ["graph-network-matrices", ["module-applications", 5]], ["differential-and-fourier", ["module-applications", 6]], ["numerical-stability", ["module-applications", 7]]
]);

const existingConceptIds = new Set((await readdir(conceptsRoot)).filter((name) => name.endsWith(".json")).map((name) => name.replace(/\.json$/, "")));
const allConceptIds = new Set([...existingConceptIds, ...unitMeta.keys()]);
const sourceRef = { source: "openstax-linear-algebra", locator: "course-wide definitions and examples" };

function prerequisitesFor(id) {
  const order = [...unitMeta.keys()];
  const index = order.indexOf(id);
  const previous = index > 0 ? order[index - 1] : null;
  const overrides = {
    "scalar-introduction": [], "vector-introduction": ["scalar-introduction"], "vector-notation": ["vector-introduction"], "vector-addition": ["vector-notation"], "scalar-multiplication": ["vector-addition"], "dot-product-and-norm": ["scalar-multiplication"],
    "linear-equations": ["dot-product-and-norm"], "vector-spaces": ["rref-and-pivots"], "linear-transformations": ["coordinate-vectors"], "orthogonality": ["composition-and-similarity"], "determinant-intuition": ["qr-least-squares"], "eigenvalue-eigenvector": ["determinant-volume"], "quadratic-forms": ["positive-definite-matrices"]
  };
  return overrides[id] ?? (previous ? [previous] : []);
}

function section(id, title, kind, body, claimId) { return { id, title, kind, body, claimRefs: [claimId] }; }
function solution(id, p, type) {
  return { id, type, difficulty: type === "calculation" ? "basic" : type === "error_detection" ? "standard" : "challenge", question: `${p.focus}を、定義と具体例を使って説明・確認せよ。`, solution: { what_is_asked: `${p.focus}について、計算結果だけでなく意味と条件を示す。`, concepts: [id], strategy: "定義を確認し、小さな例へ適用してから結果を検算する。", steps: ["問題の対象と記号の次元・条件を確認する。", `定義に沿って${p.method}`, `具体例として${p.example}`, "最後に境界条件やよくある誤りと照合する。"], why: ["定義から計算へ進むと、手順の各操作の意味を説明できる。", "検算と境界条件の確認で、形式的な答えと数学的な意味のずれを防げる。"], conclusion: p.answer, common_wrong_path: p.boundary } };
}

function unitFrom(id, module, order, p, nextId) {
  const prerequisites = prerequisitesFor(id);
  const claimId = `${id}-claim-01`;
  const exercises = [solution(`${id}-exercise-basic`, p, "calculation"), solution(`${id}-exercise-meaning`, p, "explanation"), solution(`${id}-exercise-boundary`, p, "error_detection")];
  const lessons = [
    { id: `${id}-lesson-01`, title: `${p.ja}を問題から見る`, summary: `${p.focus}を具体的な問題と小さな例からつかむ。`, objectives: [`${p.focus}を自分の言葉で説明できる`], sections: [section(`${id}-problem`, "問題：何を説明したいか", "problem", `学習の出発点は、${p.ja}を名前だけ覚えることではなく、${p.intuition} ${p.definition}`, claimId), section(`${id}-example`, "具体例", "example", p.example, claimId), section(`${id}-check`, "Checkpoint", "checkpoint", `確認：${p.focus}を一文で説明し、例の結果が定義のどの条件を使ったかを指摘してください。`, claimId)], exerciseIds: exercises.slice(0, 1).map((e) => e.id) },
    { id: `${id}-lesson-02`, title: `${p.ja}の定義と記法`, summary: "記号の役割を分解し、条件を落とさずに読む。", objectives: ["定義の各条件と記号の意味を区別できる"], sections: [section(`${id}-recall`, "前提の確認", "prerequisite_recall", prerequisites.length ? `このUnitでは${prerequisites.join("、")}を使います。前のUnitで扱った記号を思い出し、対象の次元と演算を固定します。` : "まずスカラー、ベクトル、成分の順序を確認します。", claimId), section(`${id}-definition`, "形式的な定義", "formal_definition", p.definition, claimId), section(`${id}-terms`, "記号をほどく", "term_by_term", `式に登場する対象を、${p.focus}・入力・出力・条件に分けて読みます。${p.method} 記号の意味を途中で変えないことが重要です。`, claimId), section(`${id}-check`, "Checkpoint", "checkpoint", `定義の条件を一つ外すと何が起こるか。${p.boundary}`, claimId)], exerciseIds: exercises.slice(1, 2).map((e) => e.id) },
    { id: `${id}-lesson-03`, title: `${p.ja}を計算へ移す`, summary: "手順、検算、失敗条件を一つの例で追う。", objectives: [`${p.ja}の標準的な計算手順を実行できる`, "結果を数学的な意味へ戻して説明できる"], sections: [section(`${id}-method`, "手順", "method", p.method, claimId), section(`${id}-worked`, "Worked example", "worked_example", `例題：${p.example} 手順1：対象の形状・条件を確認する。手順2：${p.method} 手順3：${p.answer}となることを代入または別の表現で検算する。結果は、${p.intuition}`, claimId), section(`${id}-failure`, "境界ケース", "counterexample", p.boundary, claimId), section(`${id}-check`, "Checkpoint", "checkpoint", `自分で説明する：${p.answer}が成立する理由を、式一つと日本語一文で書いてください。`, claimId)], exerciseIds: exercises.slice(2).map((e) => e.id) },
    { id: `${id}-lesson-04`, title: `${p.ja}を前後の概念につなぐ`, summary: "計算結果を、次の学習の問いへ接続する。", objectives: [`${p.ja}が前後のUnitで果たす役割を説明できる`], sections: [section(`${id}-relationship`, "三つの見方", "relationship", `直観では${p.intuition}、形式では${p.definition}、計算・応用では${p.method}という三層を往復します。どれか一つだけに縮めると、条件や意味を失います。`, claimId), section(`${id}-misconception`, "よくある誤解", "misconception", p.boundary, claimId), section(`${id}-summary`, "まとめ", "summary", `${p.focus}の要点は、${p.answer}。定義・例・検算を順に使うことで、見た目が似た別の問題にも転用できます。`, claimId), section(`${id}-next`, "次の接続", "next_connection", nextId ? `次は${nextId}を扱います。${p.ja}で得た${p.focus}の見方を、次の概念の前提として再利用します。` : "ここまでの概念を総合レビューで組み合わせます。", claimId)], exerciseIds: [] }
  ];
  return { id, module, order, title: { ja: p.ja, en: p.en }, summary: { ja: `${p.focus}を直観・定義・計算・境界ケースの順に学ぶ。`, en: `A complete lesson on ${p.en}, connecting intuition, definition, computation, and edge cases.` }, estimated_duration: { reading_minutes: 12, video_minutes: 10 }, concepts: { primary: [id], supporting: prerequisites.slice(0, 2) }, prerequisites, learning_objectives: [`${p.focus}を定義と具体例で説明できる`, `${p.focus}の基本計算を実行し、結果を検算できる`], content: [ { type: "motivation", body: `なぜ${p.ja}を学ぶのでしょうか。${p.intuition} ${p.focus}は、次の計算や応用で同じ問いを何度も整理するための道具です。` }, { type: "prerequisite_recall", body: prerequisites.length ? `前提：${prerequisites.join("、")}。対象の次元と記号を確認してから進みます。` : "前提：スカラー、ベクトル、成分の順序を確認します。" }, { type: "intuition", body: p.intuition }, { type: "formal_definition", body: p.definition }, { type: "term_by_term", body: `定義を、対象・操作・条件・結論に分けて読みます。${p.method}` }, { type: "worked_example", body: `例題：${p.example} まず条件を確認し、${p.method} 最後に${p.answer}と解釈します。` }, { type: "counterexample", body: p.boundary }, { type: "checkpoint", body: `確認問題：${p.focus}の定義を一文で述べ、例の計算を一行で検算してください。答えの目安は${p.answer}` }, { type: "summary", body: `${p.focus}は、${p.definition} 重要なのは${p.focus}を式だけでなく、${p.intuition}としても読めることです。` }, { type: "connection", body: nextId ? `次の${nextId}では、このUnitの結果を使って、より複雑な構造を扱います。` : "最後に、複数のModuleを横断するレビューで再利用します。" } ], exercises, formats: { html: { required: true, status: "authored" }, pdf: { required: true, status: "source_ready" }, video: { required: ["span", "linear-independence", "basis-definition"].includes(id), status: ["span", "linear-independence", "basis-definition"].includes(id) ? "scripted" : "not_planned" } }, evidence: ["openstax-linear-algebra"], visuals: [], remediation: prerequisites, status: "audited" };
}

const orderedIds = [...unitMeta.keys()];
const requiredConcepts = new Map();
for (const [id] of unitMeta) {
  const unit = JSON.parse(await readFile(path.join(unitsRoot, `${id}.json`), "utf8"));
  for (const conceptId of unit.concepts?.primary ?? []) requiredConcepts.set(conceptId, profiles[id] ?? profiles[conceptId]);
}
for (const [id, p] of requiredConcepts) {
  if (existingConceptIds.has(id)) continue;
  if (!p) throw new Error(`missing authoring profile for concept ${id}`);
  const claimId = `${id}-claim-01`;
  const concept = { id, title: { ja: p.ja, en: p.en }, summary: { ja: p.definition, en: p.definition }, prerequisites: [], related: [], learningObjectives: [`${p.focus}を定義と例で説明できる`, `${p.focus}を小さな計算へ適用できる`], teachingSequence: ["motivation", "intuition", "formal_definition", "worked_example", "counterexample", "checkpoint", "connection"], designRationale: ["具体例から形式定義へ進み、最後に計算と境界条件へ戻る。", "前後のUnitとの接続を明示して、単独カード化を避ける。"], lessons: [{ id: `${id}-concept-lesson`, title: p.ja, summary: p.focus, objectives: [`${p.focus}を説明できる`], sections: [{ id: `${id}-concept-section`, title: "定義と例", kind: "definition", body: `${p.definition} ${p.example}`, claimRefs: [claimId] }], exerciseIds: [`${id}-concept-exercise`] }], claims: [{ id: claimId, statement: p.definition, sourceRefs: [sourceRef], claimType: "definition", status: "supported" }], content: { explanation: `${p.intuition} ${p.definition} ${p.method}` }, examples: [{ id: `${id}-example`, statement: p.example, explanation: `${p.method} ${p.answer}`, type: "worked", difficulty: "basic", goal: p.focus, plan: "定義を確認して小さな例を計算する。", steps: ["対象と条件を確認する。", p.method, p.answer], finalConclusion: p.answer, whyThisWorks: "定義に沿った操作なので、結果を別表現で検算できる。", commonWrongPath: p.boundary, conceptsUsed: [id] }], exercises: [{ id: `${id}-concept-exercise`, type: "calculation", difficulty: "basic", question: `${p.focus}の小さな例を計算せよ。`, answer: p.answer, explanation: `${p.method} ${p.boundary}`, lessonId: `${id}-concept-lesson`, testsClaims: [claimId] }], misconceptions: [p.boundary], sources: ["openstax-linear-algebra"] };
  await writeFile(path.join(conceptsRoot, `${id}.json`), JSON.stringify(concept, null, 2) + "\n", "utf8");
}

for (const [id, [module, order]] of unitMeta) {
  const file = path.join(unitsRoot, `${id}.json`);
  const current = JSON.parse(await readFile(file, "utf8"));
  if (current.status !== "planned") continue;
  const primaryId = current.concepts?.primary?.[0];
  let p = profiles[id] ?? profiles[primaryId];
  if (!p && primaryId) {
    try {
      const concept = JSON.parse(await readFile(path.join(conceptsRoot, `${primaryId}.json`), "utf8"));
      p = profile(concept.title.ja, concept.title.en, concept.summary.ja, concept.content?.explanation ?? `${concept.title.ja}を、定義と例から理解する。`, "定義に含まれる対象と条件を一つずつ確認し、小さな例へ適用する。", `${concept.title.ja}の定義を満たす小さな例を考える。`, `${concept.title.ja}を名前だけで判断せず、定義の条件を確認する。`, `${concept.title.ja}の条件を満たす。`, `${concept.title.ja}の定義と例を説明する`);
    } catch { /* the explicit profile check below reports the missing authoring data */ }
  }
  if (!p) throw new Error(`missing authoring profile for unit ${id}`);
  const nextId = orderedIds[orderedIds.indexOf(id) + 1];
  await writeFile(file, JSON.stringify(unitFrom(id, module, order, p, nextId), null, 2) + "\n", "utf8");
}
console.log(`Authored ${[...unitMeta.keys()].filter((id) => profiles[id]).length} v2.0 unit profiles and knowledge concepts.`);
