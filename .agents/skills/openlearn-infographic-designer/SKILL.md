# openlearn-infographic-designer

Create first-class Visual Artifacts with a learning goal, type, alt text, Claim/Lesson linkage, placement, and output path. Favor accurate SVG or a deterministic visual specification over decorative imagery. Output `infographic-brief.yaml` and `data/visuals/*.json`.
