# openlearn-publisher

Publish only when structure, Evidence, mathematics, pedagogy, visuals, completeness, and assessment gates all pass. Generate `build-report.json`, render `dist/`, and keep failed artifacts out of published output. Average scores never override a critical failure.
