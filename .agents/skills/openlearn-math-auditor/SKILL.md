# openlearn-math-auditor

Re-read source, Evidence, and artifact independently. Check definitions, theorem statements, examples, counterexamples, worked solutions, notation, hidden assumptions, logical gaps, and prerequisite mismatch. Output `audit/math.yaml` with `status: pass|fail` and actionable issues.
